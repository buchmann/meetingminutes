# Sync the LE wildcard from Vault into local-ai/local-ai-tls (type kubernetes.io/tls).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wildcard-tls
  namespace: local-ai
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: local-ai-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  data:
    - secretKey: tls.crt
      remoteRef: { key: certs/wildcard-allwaysbeginner, property: tls.crt }
    - secretKey: tls.key
      remoteRef: { key: certs/wildcard-allwaysbeginner, property: tls.key }
