apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-ai-data
  namespace: local-ai
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
